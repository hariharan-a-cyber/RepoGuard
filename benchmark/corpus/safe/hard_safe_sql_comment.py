def docs():
    # This function does NOT run SQL. The line below is a docstring example.
    help_text = "Use SELECT * FROM table to read rows; never build it with + concatenation."
    return help_text
